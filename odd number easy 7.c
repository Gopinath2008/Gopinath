#include<Stdio.h>
int main()
{
	int num;
	printf("enter the number:");
	scanf("%d",&num);
	if(num%2!=0)
	printf("the given number is odd number");
	else
	printf("the given number is even number:");
}
